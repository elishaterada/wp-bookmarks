/*global $:false */

$(function() {
    var options = {
        bookmarks_source: './sample.json',
        tmpl_path: 'tmpls/',
        tmpl_cache: true,
    };

    // var wp_bookmarks = $('#wp-bookmarks').wp_bookmarks(options);
});
